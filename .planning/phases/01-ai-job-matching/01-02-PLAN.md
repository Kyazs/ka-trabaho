---
phase: 01-ai-job-matching
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/App.tsx
  - src/components/Navbar.tsx
  - src/index.css
autonomous: true
requirements:
  - JOB-03
  - JOB-04
  - JOB-05
must_haves:
  truths:
    - User can see job results in Job Market tab
    - Job cards show salary, demand, and required courses
    - Cross-navigation works between jobs, courses, and chat
    - Job count badge appears on Navbar when results exist
    - Course matching results include "View Matching Jobs" button
  artifacts:
    - path: src/App.tsx
      provides: "Job Market tab UI and handlers"
      contains: "currentTab === 'jobs'"
    - path: src/components/Navbar.tsx
      provides: "Job tab navigation with badge"
      contains: "tab-jobs-btn"
  key_links:
    - from: "Job Market tab"
      to: "Explorer tab"
      via: "setCurrentTab('explorer') + setSelectedSector"
      pattern: "setCurrentTab\(.*explorer.*\)"
    - from: "Course matching results"
      to: "Job Market tab"
      via: "handleSubmitJobMatching + setCurrentTab('jobs')"
      pattern: "handleSubmitJobMatching\(\)"
---

## Objective

Complete the frontend integration of the Job Market feature. Add missing cross-tab navigation, job count badges, and integration buttons between course matching and job matching.

Purpose: Users need seamless navigation between courses and jobs, plus visual feedback (badges) when job matches are available. This ties the entire job matching feature together into a cohesive experience.

Output: Fully integrated Job Market tab with all navigation links and badges working correctly.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/App.tsx
@src/components/Navbar.tsx

### Key Interfaces

From src/App.tsx (lines 73-76, 247-287, 1380-1584):
- `isJobMatching`, `jobMatchResult`, `jobMatchError` states exist
- `handleSubmitJobMatching` handler exists and calls `/api/job-recommendation`
- Job Market UI exists in `currentTab === 'jobs'` block with job cards, required courses, chat button
- `askChatAboutJob` function exists (line 377)

From src/components/Navbar.tsx (lines 71-80):
- Jobs tab button exists with `id="tab-jobs-btn"` and `Briefcase` icon
- No job count badge currently implemented

Missing features (from original implementation plan, not yet in code):
1. Job count badge on Navbar Jobs tab (when jobMatchResult exists)
2. "View Matching Jobs" button in course matching results (after course cards)
3. "View Courses" button in each job result card (to navigate to Explorer)

## Tasks

<task type="auto">
  <name>Task 1: Add cross-navigation and integration buttons</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (lines 700-900, course matching results section)
    - src/App.tsx (lines 1380-1584, job market tab section)
    - src/App.tsx (lines 247-287, handleSubmitJobMatching handler)
  </read_first>
  <action>
    Add missing integration features to src/App.tsx per D-03 and D-04:
    1. In course matching results (after the course cards grid, before enrollment tips card), add a "View Matching Jobs" button that calls `handleSubmitJobMatching()` then `setCurrentTab('jobs')`. Use the same button styling as specified in the original plan (emerald gradient with Briefcase icon). per D-03
    2. In each job result card (after the required courses section, before the chat button), add a "View Courses" button that navigates to the Explorer tab with the correct sector selected. Use `setSelectedSector(sector)` where sector is found from `SECTORS_DATA.find(s => s.id === job.sectorId)`. per D-04
    3. Ensure bilingual Taglish/English text is used for all new buttons. per D-03
    4. Add proper imports: `BookOpen` is already imported in App.tsx, verify `Briefcase` is imported (it is, line 17). per D-06
    5. Verify all navigation state changes work correctly and don't cause React errors
  </action>
  <verify>
    <automated>
      grep -c "View Matching Jobs" src/App.tsx && grep -c "View Courses" src/App.tsx && grep -c "Tingnan ang Sector" src/App.tsx
    </automated>
  </verify>
  <done>
    - "View Matching Jobs" button appears in course matching results with correct styling
    - "View Courses" button appears in each job card and navigates to correct sector
    - Both buttons use bilingual text (Taglish/English) based on `lang` state
    - Navigation works without React errors or state conflicts
  </done>
</task>

<task type="auto">
  <name>Task 2: Add job count badge to Navbar</name>
  <files>src/components/Navbar.tsx</files>
  <read_first>
    - src/components/Navbar.tsx (full file, to see props and current tab structure)
  </read_first>
  <action>
    Add job count badge to the Navbar Jobs tab in src/components/Navbar.tsx per D-05:
    1. Add a `jobCount` or `jobMatchResult` prop to NavbarProps interface so the Navbar can receive job result data. Alternatively, use the existing `hasProfile` prop pattern.
    2. Add a notification badge on the Jobs tab button that shows the number of recommended jobs when:
       - `jobMatchResult?.recommendedJobs?.length > 0`
       - AND the current tab is NOT "jobs" (only show badge when user is on another tab)
    3. Badge styling: absolute positioned `-top-1 -right-1`, emerald-500 background, white text, small circular badge (w-5 h-5, text-[10px])
    4. Ensure badge is responsive and doesn't break mobile menu layout
    5. Add `jobMatchResult` to NavbarProps if needed, and update the parent App.tsx to pass this prop
  </action>
  <verify>
    <automated>
      grep -c "absolute -top-1" src/components/Navbar.tsx && grep -c "jobMatchResult" src/components/Navbar.tsx
    </automated>
  </verify>
  <done>
    - Navbar shows job count badge when jobs are available and user is on another tab
    - Badge disappears when user navigates to Jobs tab
    - Badge is hidden when no job results exist
    - Mobile menu version also works correctly
    - TypeScript types are updated without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify UI states and responsive design</name>
  <files>src/App.tsx, src/components/Navbar.tsx, src/index.css</files>
  <read_first>
    - src/App.tsx (lines 1380-1450, job matching form and loading states)
    - src/App.tsx (lines 1450-1520, error states)
  </read_first>
  <action>
    Verify and refine UI/UX for the Job Market tab per D-05:
    1. Verify loading state (`isJobMatching`) displays correct spinner and text in both languages
    2. Verify disabled state (when no interests or career goal) shows warning message with correct bilingual text
    3. Verify error state displays in Taglish/English correctly with `AlertCircle` icon
    4. Verify job cards are responsive: grid-cols-1 on mobile, grid-cols-2 on desktop (md:grid-cols-2)
    5. Verify `animate-fade-in` class exists in src/index.css (add if missing)
    6. Check that all interactive elements have proper `id` attributes for testing (btn-submit-job-matching, tab-jobs-content, etc.)
    7. Verify job card hover effects work with `hover:shadow-md transition-all`
    8. Add any missing CSS utilities needed for the job UI
  </action>
  <verify>
    <automated>
      grep -c "animate-fade-in" src/index.css && grep -c "btn-submit-job-matching" src/App.tsx && grep -c "job-results-section" src/App.tsx
    </automated>
  </verify>
  <done>
    - All loading, error, and disabled states render correctly
    - Job cards are responsive (1 column mobile, 2 column desktop)
    - animate-fade-in animation exists and works
    - All key elements have testable IDs
    - No console errors or TypeScript issues
    - UI matches existing app design language (rounded corners, gradients, card styles)
  </done>
</task>

## Threat Model

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→frontend | User input via form and button clicks |
| frontend→backend | fetch() calls to /api/job-recommendation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Spoofing | UI state | accept | Client-side state, no auth |
| T-01-02-02 | Tampering | API requests | mitigate | Content-Type headers set correctly |
| T-01-02-03 | Information Disclosure | Error messages | mitigate | Errors shown in Taglish, no stack traces exposed to UI |
| T-01-02-04 | Denial of Service | Client requests | accept | No rate limiting on client side |

## Verification

- [ ] All new buttons appear in correct locations
- [ ] Navigation between tabs works smoothly
- [ ] Job count badge displays correctly
- [ ] Responsive design works on mobile and desktop
- [ ] TypeScript compiles without errors (`npm run lint`)

## Success Criteria

1. User can click "View Matching Jobs" from course results to see job matches
2. User can click "View Courses" from a job card to see relevant courses in Explorer
3. Navbar shows job count badge when matches exist
4. All UI states (loading, error, disabled) work correctly in both languages
5. TypeScript compilation passes without errors
6. Mobile responsive design works correctly

## Output

Create `.planning/phases/01-ai-job-matching/01-02-SUMMARY.md` when done
