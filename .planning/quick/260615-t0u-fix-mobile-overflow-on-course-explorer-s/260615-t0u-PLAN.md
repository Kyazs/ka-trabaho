---
phase: 260615-t0u-fix-mobile-overflow-on-course-explorer-s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
autonomous: true
requirements:
  - QUICK-260615-t0u
must_haves:
  truths:
    - Mobile course explorer no longer shows a horizontally overflowing sector chip list
    - Mobile sector selector is a vertical/icon list with larger touch targets and no horizontal scroll
    - Selecting a sector on mobile still updates selectedSector and renders the sector details
    - Search filter and empty state still work on mobile and desktop
    - Desktop vertical sector list remains visually unchanged
    - Build and TypeScript checks pass
  artifacts:
    - path: src/App.tsx
      provides: Responsive mobile sector selector replacing the horizontal chip list
      min_lines: 700
  key_links:
    - from: mobile sector selector buttons
      to: selectedSector state
      via: onClick={() => setSelectedSector(sector)}
      pattern: setSelectedSector
    - from: explorer-left-sectors container
      to: explorer-right-details container
      via: selectedSector shared state
      pattern: selectedSector
---

<objective>
Replace the horizontally overflowing mobile sector chip list in the Course Explorer with a responsive, mobile-friendly vertical icon list that matches the desktop design but is adapted for compact touch screens.

Purpose: Improve mobile UX by eliminating horizontal overflow and providing clear, tappable sector choices with icons and course counts.
Output: Updated `src/App.tsx` with the mobile sector selector redesigned and verified build.
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/references/planner-antipatterns.md
@src/App.tsx
@src/index.css
@src/components/LandingPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace mobile horizontal chip list with responsive vertical icon sector list</name>
  <files>src/App.tsx</files>
  <action>
    In the Course Explorer tab (`currentTab === "explorer"`), replace the mobile-only horizontal chip list (`<div className="flex overflow-x-auto gap-2 pb-2 lg:hidden">`, around lines 700–715) and the desktop-only left sector panel (`<div id="explorer-left-sectors" className="hidden lg:block lg:col-span-4 space-y-3">`, around lines 720–770) with a single responsive sector selection panel that:

    - Is visible on all viewports (remove `hidden lg:block` from the sector panel and delete the `lg:hidden` chip list).
    - Keeps the same heading: `{lang === "fil" ? "Pumili ng Sektor na Gusto:" : "Choose a Vocational Sector:"}`.
    - Keeps the same button structure, IDs (`id={`btn-explorer-sector-${sector.id}`}`), onClick handler (`() => setSelectedSector(sector)`), and selected-state styling (`bg-[#0F3D91] text-white`, `bg-white text-[#1A1A2E]`, `bg-[#E8F0FE]`, etc.).
    - Uses the existing `getSectorIcon(sector.iconName)` icon and the existing course count text (`sector.courses.length` + bilingual label).
    - On mobile (`default` to `md:`): renders the sector buttons in a 1- or 2-column grid layout with larger touch targets (minimum `min-h-[64px]` or `p-4`), using the existing flex row style inside each button (icon left, text right). Ensure text does not wrap awkwardly; use `min-w-0` on text blocks if needed.
    - On desktop (`lg:`): retains the original vertical `space-y-2` stacked list with the same full-width buttons.
    - Preserves the empty-state block when `filteredSectors.length === 0`, including the `BadgeHelp` icon and the `btn-clear-search` button.

    Do not change the parent `grid grid-cols-1 lg:grid-cols-12 gap-8` layout; the sector panel should still occupy `lg:col-span-4` and the details panel `lg:col-span-8`. On mobile, the sector panel should be a full-width column that sits above the details panel.

    Use only the ka-trabaho theme colors: `#0F3D91`, `#FCD116`, `#E8F0FE`, `#e5e8ef`, `#1A1A2E`, `#6B7280`. No arbitrary new colors. Use Tailwind utility classes; no inline styles. Use only Lucide icons already imported in `getSectorIcon`.

    After editing, run the build and type checks to confirm no regressions.
  </action>
  <verify>
    <automated>npm run build && npx tsc --noEmit</automated>
  </verify>
  <done>
    - The mobile horizontal chip list is removed.
    - A vertical/icon sector list is visible on mobile with no horizontal overflow and tappable touch targets.
    - Desktop layout remains the same vertical stacked list.
    - `npm run build` and `npx tsc --noEmit` pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI | Untrusted viewport/input; layout only, no sensitive data crossing this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260615-t0u-01 | Information Disclosure | Mobile layout | accept | No PII or sensitive data in this component; layout change only |
| T-260615-t0u-02 | Tampering | src/App.tsx | accept | UI-only change; selection logic and state handling unchanged |
| T-260615-t0u-03 | Denial of Service | Build verification | mitigate | Run `npm run build && npx tsc --noEmit` before completion |
</threat_model>

<verification>
1. `npm run build` passes without errors.
2. `npx tsc --noEmit` reports zero TypeScript errors.
3. Visually inspecting the Explorer tab on a mobile viewport (≤768px) shows a vertical/icon sector list without horizontal scrolling.
4. Clicking a mobile sector item updates the details panel on the right.
5. The search filter and clear-search button still work.
</verification>

<success_criteria>
- The mobile Course Explorer sector selector no longer overflows horizontally.
- Mobile users can tap a sector item (with icon and course count) to view its details.
- Desktop layout and behavior are unchanged.
- All existing IDs, event handlers, and bilingual text remain intact.
- Build and TypeScript checks pass.
</success_criteria>

<output>
Create `.planning/quick/260615-t0u-fix-mobile-overflow-on-course-explorer-s/260615-t0u-SUMMARY.md` when done.
</output>
